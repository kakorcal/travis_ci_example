const expect = require('chai').expect;

describe('Adding', ()=>{
  it('Should equal one', ()=>{
    expect(1 + 0).to.equal(1);
  });

  it('Should equal two', ()=>{
    expect(1 + 1).to.equal(2);
  });
});